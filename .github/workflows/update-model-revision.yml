name: Update Model Revision

on:
  schedule:
    # Run weekly on Monday at 04:00 UTC
    - cron: "0 4 * * 1"
  workflow_dispatch:

env:
  MODEL_REPO: "desert-ant-labs/gist"

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v10.1.0
        with:
          version: "0.12.16"

      - name: Install dependencies
        run: uv sync --python 3.14 --extra dev --extra litert

      - name: Get latest model revision
        id: revision
        run: |
          uv run scripts/get_latest_revision.py > /tmp/revision_output.txt
          cat /tmp/revision_output.txt
          CURRENT=$(grep "^current=" /tmp/revision_output.txt | cut -d= -f2)
          LATEST=$(grep "^latest=" /tmp/revision_output.txt | cut -d= -f2)
          echo "current=$CURRENT" >> $GITHUB_OUTPUT
          echo "latest=$LATEST" >> $GITHUB_OUTPUT

      - name: Check if update needed
        id: check
        run: |
          CURRENT="${{ steps.revision.outputs.current }}"
          LATEST="${{ steps.revision.outputs.latest }}"
          echo "current=$CURRENT"
          echo "latest=$LATEST"

          if [ "$CURRENT" = "$LATEST" ]; then
            echo "update_needed=false" >> $GITHUB_OUTPUT
            echo "Already at latest revision: $CURRENT"
          else
            echo "update_needed=true" >> $GITHUB_OUTPUT
            echo "Newer revision available: $LATEST (current: $CURRENT)"
          fi

      - name: Exit if no update needed
        if: steps.check.outputs.update_needed == 'false'
        run: |
          echo "Repository is already at the latest model revision."
          exit 0

      - name: Update model revision file
        run: |
          LATEST="${{ steps.revision.outputs.latest }}"
          echo "$LATEST" > src/taxotag/.model-revision
          echo "Updated src/taxotag/.model-revision to $LATEST"

      - name: Verify changes
        run: |
          echo "=== src/taxotag/.model-revision ==="
          cat src/taxotag/.model-revision

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v8
        with:
          branch: model-revision-${{ steps.revision.outputs.latest }}
          branch-suffix: short-commit-hash
          commit-message: "fix: update model revision to ${{ steps.revision.outputs.latest }}"
          title: "fix: update model revision to ${{ steps.revision.outputs.latest }}"
          body: |
            ## Model Revision Update

            Automated PR to update the model revision from `${{ steps.revision.outputs.current }}` to `${{ steps.revision.outputs.latest }}`.

            ### Changes

            - Updated `src/taxotag/.model-revision` file

            ### Verification Required

            Please verify:
            1. The new model version is compatible with the codebase
            2. All tests pass with the updated model
            3. No breaking changes were introduced

            ---

            *This PR was automatically generated by the `Update Model Revision` workflow.*
          labels: |
            automated
            model-update
          delete-branch: true
